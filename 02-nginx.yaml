- name: nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
    - name: nginx
      state: present
    
  - name: service enable
    ansible.builtin.service:
    - name: nginx
      state: presented
      enabled: yes
